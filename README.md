# Plant Disease Classifier

Классификатор болезней растений на основе сверточной нейронной сети ResNet18 с Telegram-ботом.

## Описание

Проект определяет болезни растений по фотографии листа. Модель обучена на датасете PlantVillage (38 классов болезней). Включает консольный скрипт для предсказаний, Telegram-бота и собственный API с фронтендом для удобного взаимодействия. 

## Возможности

- Классификация 38 видов болезней растений
- Transfer learning с использованием ResNet18
- Поддержка GPU для ускорения обучения
- Автоматическое сохранение лучшей модели
- Telegram-бот для получения предсказаний
- API 
- Веб-часть для обычных пользователей

## Структура проекта

```
plants_analysis/
├── .env                    # Переменные окружения (TOKEN)
├── .gitignore
├── README.md
├── requirements.txt
├── translations.json       # Небольшой файл для получения перевода болезней на русский
├── error_classes.json      # Проблемные классы  
├── src/
│   ├── frontend/
│   │   ├── index.html
│   │   ├── style.css
│   │   └── script.js
│   ├── download.py         # Загрузка датасета из kaggle
│   ├── model.py            # Архитектура модели
│   ├── analyze_errors.py   # Файл для нахождения проблемных классов
│   ├── train.py            # Обучение модели
│   ├── predict.py          # Логика предсказания
│   ├── data_loader.py      # Загрузка и аугментация данных
│   ├── prepare_data.py     # Подготовка датасета
│   ├── api.py              # API и бэкенд
│   ├── check_classes.py    # Проверка классов (была необходима на моменте ошибки с их добавлением)
│   └── app.py              # Telegram-бот
├── models/
│   └── model.pth       # Обученная модель
├── data/
│   ├── raw/                # Исходные данные
│   └── processed/          # Обработанные данные (train/val)
└──           
```

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/fan1omas/plants_analysis
cd plants_analysis
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Подготовьте данные:
```bash
python src/prepare_data.py
```

4. Настройте переменные окружения:
```bash
# Создайте файл .env в корне проекта
echo "TOKEN=your_telegram_bot_token" > .env
```

## Использование

### Обучение модели

```bash
python src/train.py
```

Параметры обучения настраиваются в `src/train.py`:
- NUM_EPOCHS: 10
- LEARNING_RATE: 0.001
- BATCH_SIZE: 32

### Предсказание через консоль

```bash
python src/predict.py
```

Укажите путь к изображению в `predict.py`:
```python
IMAGE_PATH = Path("path/to/your/image.jpg")
```

### Запуск Telegram-бота

```bash
python src/app.py
```

### API и фронтенд
```bash
python src/api.py          (localhost:8000)
python -m http.server 3000 (localhost:3000)
```
```
curl -X POST http://localhost:8000/predict \
  -F "file=@leaf.jpg"
```

Получите токен бота у @BotFather в Telegram и добавьте в `.env`.

## Результаты

| Метрика | Значение |
|---------|----------|
| Точность на валидации | 85-90% |
| Точность на реальных фото | 40-60% |
| Размер модели | 40 MB |
| Время обучения (GPU) | 30-50 минут |
| Время обучения (CPU) | 5-10 часов |
| Время предсказания | 100-200 мс |

## Ограничения

Модель обучена на датасете PlantVillage, который снимался в лабораторных условиях. Это влияет на точность при работе с реальными фотографиями. Скорее всего модель правильно назовет тип растения но неправильно выберет заболевание.

Почему точность ниже на реальных фото:
- В датасете однородный фон, в реальности фон разнообразный
- В датасете студийное освещение, в реальности разное освещение
- В датасете лист в центре кадра, в реальности разный ракурс

Рекомендации для лучших результатов:
- Фотографируйте лист на однородном фоне
- Используйте хорошее освещение
- Заполняйте листом большую часть кадра

В новом файле error_classes.json можно узнать, какие классы чаще всего «ошибаются» и их нужно дообучать. Также можно узнать, какие ложные предсказания давала модель. К примеру, класс с гнилью томата ошибается 561 раз из 1000, поэтому в дальнейшем модель будет дообучаться на конкретных примерах для улучшения статистики. 

## Технические детали

Архитектура:
- Base: ResNet18 (предобученная на ImageNet)
- Замороженные слои: все кроме последнего полносвязного слоя
- Обучаемые параметры: около 19,000
- Входное изображение: 224x224 RGB

Аугментация данных:
- Случайный поворот (30 градусов)
- Горизонтальное и вертикальное отражение
- ColorJitter (яркость, контраст, насыщенность, тон)
- Normalization (ImageNet mean/std)

Оборудование:
- Рекомендуется GPU (CUDA)
- Протестировано на Google Colab T4

## Зависимости

```
torch>=2.0.0
torchvision>=0.15.0
Pillow>=9.0.0
numpy>=1.24.0
aiogram>=3.0.0
python-dotenv>=1.0.0
fastapi[standard]>=0.109.0
```

## Планы улучшения

- Сбор и разметка реальных полевых данных
- Дообучение модели на реальных фотографиях
- Уменьшение размера модели для деплоя